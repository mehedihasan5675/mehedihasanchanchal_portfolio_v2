const SectionTitle = ({ title }) => {
  return (
    <div>
      <h2 className="text-slate-700 text-2xl lg:text-4xl my-3 font-semibold tracking-wider">
        {title}
      </h2>
    </div>
  );
};

export default SectionTitle;
